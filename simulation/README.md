# Simulation

This directory is the center of the rebuild.

- `core/` is for runtime simulation logic
- `scenarios/` is for scenario data and presets
- `calibration/` is for ingest, fitting, and asset preparation
