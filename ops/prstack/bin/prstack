#!/usr/bin/env bash

set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/../../.." && pwd)"
export PRSTACK_PROJECT_ROOT="${ROOT_DIR}"

exec /home/prithiv/Prithiv_Projects/prstack/bin/prstack "$@"
